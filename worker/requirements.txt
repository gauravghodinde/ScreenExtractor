grpcio>=1.56.0
## Lightweight worker requirements (CPU-only) -- keep these minimal for quick build
# ASR / GPU libs removed for now; add faster-whisper and torch when enabling GPU/ASR

# Video processing
ffmpeg-python>=0.2.0

# Storage / S3-compatible
minio>=7.1.5

# Redis queue
redis>=4.5.0

# gRPC
grpcio>=1.56.0
protobuf>=4.23.0

# Postgres client
psycopg[binary]>=3.1

# Utilities
python-dotenv>=1.0.0

# Text embedding and vector DB (local)
sentence-transformers>=2.2.2
chromadb>=0.3.26
srt>=0.7.6
numpy>=1.26.0
fastapi>=0.95.0
uvicorn>=0.22.0
